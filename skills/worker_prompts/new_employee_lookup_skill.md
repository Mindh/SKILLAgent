[목적]
사용자 질의에서 신규 입사자 ID(employee_id)를 추출한다.

[분석 규칙]
- 입사자 ID 형식: 'N-YYYY-숫자' (예: N-2025-001). 대소문자 구분 없음.
- 본문에 ID가 없으면 빈 문자열로 둔다.
- 이름만 있고 ID가 없으면 빈 문자열로 둔다 (DB는 ID로만 조회).

[출력 형식]
반드시 아래 JSON 형식만 출력하라. 다른 말은 덧붙이지 마라.

```json
{
  "employee_id": "N-2025-001"
}
```
